apiVersion: apps/v1
kind: Deployment
metadata:
  name: eventloader
  labels:
    app: eventloader-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: eventloader-service
  template:
    metadata:
      labels:
        app: eventloader-service
    spec:
      containers:
      - name: eventloader-service
        image: vareger/event-loader:latest
      imagePullSecrets:
      - name: regcred
